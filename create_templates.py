def create_device_master_template(file_path):\n    import pandas as pd\n\n    # Create a Settings sheet with exchange rate\n    settings_data = {'Exchange Rate': [1.0]}\n    settings_df = pd.DataFrame(settings_data)\n\n    # Create a Device Master sheet with headers and sample data\n    device_master_data = {\n        'Type': ['Assy', 'Turnkey', 'Test'],\n        'Model': ['Model A', 'Model B', 'Model C'],\n        'Price': [1000, 1500, 2000],\n    }\n    device_master_df = pd.DataFrame(device_master_data)\n\n    # Create an Instructions sheet\n    instructions = [\n        '1. Update the exchange rate in the Settings sheet.',\n        '2. Fill in the Device Master sheet with your actual data.',\n        '3. Save the file as required.'\n    ]\n    instructions_df = pd.DataFrame(instructions, columns=['Instructions'])\n\n    # Write to an Excel file\n    with pd.ExcelWriter(file_path) as writer:\n        settings_df.to_excel(writer, sheet_name='Settings', index=False)\n        device_master_df.to_excel(writer, sheet_name='Device Master', index=False)\n        instructions_df.to_excel(writer, sheet_name='Instructions', index=False)\n\n    print('Device_Master_Template.xlsx has been created.')\n